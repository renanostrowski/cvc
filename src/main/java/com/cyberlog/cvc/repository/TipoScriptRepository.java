package com.cyberlog.cvc.repository;

import com.cyberlog.cvc.models.TipoScript;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoScriptRepository extends JpaRepository<TipoScript, Integer>{
    
}
